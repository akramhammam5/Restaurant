--Step 1:
    Django works as follows: Models -- views -- Templates where views take the models and templates then view it to the user...
--Step 2:
    Django Template Language: A language used to interact with template files..

--Models:
    .Models are used for Database Storage and Processing.
    .Any DB attribute should have a DataType to avoid wrong data entry.
    .class describe the main model that contains the information.
    .Admin Will save a lot of work as it is a Django feature.
    .Relations are: 1:1, 1:m(and Vice Versa) and m:m.

--Image Display:
    .First you have to active Media.
    .Configure settings by adding media root and url.
    .Apply Configurations through urls.py from the project file
    